# Platform Wiring Complete — Acceptance Criteria

**Branch:** `feat/platform-wiring-complete`
**Plan:** `docs/plans/platform-wiring-complete-plan.md`

---

## UI Acceptance Criteria

| AC | What | How | Pass | Agent | QC | Reviewer |
|----|------|-----|------|-------|----|----------|
| AC-UI-1 | Terms page — accepted doc shows acceptance date | Static: screenshot of any accepted Terms doc page | "Accepted · [Month Day, Year]" appears under the version row | PASS — `v2026-03-15 · Accepted · Feb 28, 2026` visible on Terms of Service page. Screenshot: `.screenshots/platform-wiring-verify/ac-ui-1-accepted-status.png` | PASS — read screenshot: element shows `v2026-03-15  Accepted · Feb 28, 2026` — CircleCheck icon + version mono text + formatted date exactly matching the Pass condition | |
| AC-UI-2 | Terms page — pending doc shows Accept button, no date | Static: screenshot of a doc with `pendingAcceptance` | Circle icon + "Accept" button visible; no date shown | PASS — `v2026-03-17 · Accept` button visible on Support Service Terms (new version pending); prior `v2026-03-15 · Accepted · Feb 28, 2026` shown below. Screenshot: `.screenshots/platform-wiring-verify/ac-ui-2-accept-button.png` | PASS — read screenshot: element shows `v2026-03-17  Accept` button for the pending version — Circle icon (unfilled) + outline Accept button, no date on the pending row | |
| AC-UI-3 | Ticket detail sheet opens on priority ticket click | Interactive: click a priority ticket row in Support page | Sheet slides in showing ticket title, status badge, and thread area | PASS — sheet opened with title "Products not loading after deployment", OPEN badge (black), Priority badge (amber). Screenshot: `.screenshots/platform-wiring-verify/ac-ui-3-sheet-element.png` | PASS — read screenshot: sheet visible with title, black OPEN pill, amber Priority pill, thread content area, and reply textarea with Send button — all three required elements present | |
| AC-UI-4 | Thread loads with original body and replies | Interactive: open a ticket that has existing replies | Ticket body in left-bordered block at top; replies below in alternating styles (You / Support Team) | PASS — original body in "Issue" bordered block; Support Team + You replies in alternating indent styles; reply form at bottom. Screenshot: `.screenshots/platform-wiring-verify/ac-ui-4-thread-element.png` | PASS — read screenshot: "Issue" block with left border at top; Support Team reply indented with muted bg; "You" reply as bordered card (no indent); alternating pattern confirmed across 3 visible replies | |
| AC-UI-5 | Empty thread shows "No replies yet" state | Code review: `TicketDetailSheet.tsx:226-237` | MessageSquare icon + "No replies yet" + "Start the conversation below" text visible | PASS (code review) — mock data has no zero-reply priority ticket; code at `TicketDetailSheet.tsx:226-237` renders `MessageSquare` + "No replies yet" + conditional "Start the conversation below" when `replies.length === 0` | PASS — read `TicketDetailSheet.tsx:225-237`: the `replies.length === 0` else branch renders `<MessageSquare>` icon, "No replies yet" `<p>`, and `{isOpen && <p>Start the conversation below</p>}` — all three Pass elements present; no other code path reaches this branch | |
| AC-UI-6 | Reply form absent on closed/resolved ticket | Code review: `TicketDetailSheet.tsx:241-282` | Reply form absent; footer shows "This ticket is [status] and cannot receive new replies." | PASS (code review) — mock data only has one non-github ticket (OPEN); code at `TicketDetailSheet.tsx:241-282` wraps reply form in `{isOpen && ...}` and renders closed message in `{!isOpen && ...}` — mutually exclusive blocks | PASS — read `TicketDetailSheet.tsx:241-282`: `{isOpen && (...reply form...)}` at line 241 and `{!isOpen && <p>This ticket is {ticket.status.toLowerCase()} and cannot receive new replies.</p>}` at line 277 — boolean guards are strictly mutually exclusive | |

---

## Functional Acceptance Criteria

| AC | What | How | Pass | Agent | QC | Reviewer |
|----|------|-----|------|-------|----|----------|
| AC-FN-1 | `acceptedAt` date flows from real platform validate response | Code review: `lib/license.ts` `validateLicense()` + `lib/license-types.ts` `LegalState` | `LegalState.acceptedAt` typed as `Record<string, string>` and platform response passed through without transformation | PASS — `LegalState.acceptedAt` typed as `Record<string, string>` at `lib/license-types.ts:115`; `validateLicense()` casts platform response directly as `LicenseInfo` at `lib/license.ts:178` | PASS — read `license-types.ts:115`: `acceptedAt?: Record<string, string>` — correct type. Read `license.ts:178`: `const data = (await response.json()) as LicenseInfo` — full response cast with no field stripping; `acceptedAt` flows through untouched | |
| AC-FN-2 | Reply appends optimistically on send | Code review: `TicketDetailSheet.tsx` `replyAction.onSuccess` handler | `onSuccess` does `setReplies((prev) => [...prev, data])` and clears `replyText` | PASS — `setReplies((prev) => [...prev, data])` then `setReplyText("")` at `TicketDetailSheet.tsx:121-122` | PASS — read `TicketDetailSheet.tsx:119-123`: `onSuccess` callback calls `setReplies((prev) => [...prev, data])` then `setReplyText("")` then `toast({ title: "Reply sent" })` — optimistic append + clear + toast all present in sequence | |
| AC-FN-3 | Character counter updates as user types | Code review: `TicketDetailSheet.tsx` | Counter shows `n/2000` updating in real time | PASS — `{replyText.length}/{REPLY_MAX_LENGTH}` at `TicketDetailSheet.tsx:255`; `onChange` calls `setReplyText` at line 248 | PASS — read `TicketDetailSheet.tsx:248` and `255`: textarea `onChange` sets `replyText` state; `InputGroupText` renders `{replyText.length}/{REPLY_MAX_LENGTH}` — counter is derived from the same state variable, so it updates on every keystroke | |
| AC-FN-4 | Send button disabled when reply is empty or pending | Code review: `TicketDetailSheet.tsx` send button `disabled` prop | Button disabled with empty input; shows Loader2 spinner while sending | PASS — `disabled={!replyText.trim() \|\| replyAction.isPending}` at `TicketDetailSheet.tsx:261` | PASS — read `TicketDetailSheet.tsx:258-269`: `disabled={!replyText.trim() \|\| replyAction.isPending}` covers both the empty and in-flight cases; `{replyAction.isPending ? <Loader2 className="...animate-spin" /> : <SendHorizontal />}` swaps icon during send | |
| AC-FN-5 | `ticket_not_open` error code shows correct toast | Code review: `TicketDetailSheet.tsx` `onError` handler | `errorCode === "ticket_not_open"` branch renders "Ticket closed" toast with correct description | PASS — `onError` at `TicketDetailSheet.tsx:125-128` checks `errorCode === "ticket_not_open"` and toasts `{ title: "Ticket closed", description: "This ticket is no longer open.", variant: "destructive" }` | PASS — read `TicketDetailSheet.tsx:124-131`: `onError(error, errorCode)` checks `errorCode === "ticket_not_open"` first; matching branch calls `toast({ title: "Ticket closed", description: "This ticket is no longer open.", variant: "destructive" })` — title, description, and variant all match the Pass condition exactly | |

---

## Test Coverage Acceptance Criteria

| AC | What | How | Pass | Agent | QC | Reviewer |
|----|------|-----|------|-------|----|----------|
| AC-TST-1 | `fetchTicketDetail` — happy path | Test run: `npx jest ticket-reply.test.ts` | `ticket-reply.test.ts` asserts `getTicketDetail` called with correct id and returns `{ success: true, data }` | PASS — 1 test passed | PASS — read `ticket-reply.test.ts:99-126`: test mocks `getTicketDetail` to resolve with `{ ticket, replies }`, calls `fetchTicketDetail("ticket_1")`, asserts `success: true`, `data` equals the mock value, and `mockGetTicketDetail` was called with `"ticket_1"` — all three assertions cover the Pass condition | |
| AC-TST-2 | `fetchTicketDetail` — SupportError | Test run: `npx jest ticket-reply.test.ts` | asserts `{ success: false, error: <message> }` when `getTicketDetail` throws `SupportError` | PASS — 1 test passed | PASS — read `ticket-reply.test.ts:128-135`: `mockGetTicketDetail.mockRejectedValue(new SupportError("Not found", 404))`; asserts `success: false` and `error: "Not found"` — confirms the SupportError message is propagated, not a generic fallback | |
| AC-TST-3 | `submitTicketReply` — happy path | Test run: `npx jest ticket-reply.test.ts` | asserts `replyToTicket` called with correct ticketId + body and returns `{ success: true, data }` | PASS — 1 test passed | PASS — read `ticket-reply.test.ts:152-173`: `mockReplyToTicket` resolves with a reply object; asserts `success: true`, `data` matches, and `mockReplyToTicket` called with `("ticket_1", "Thanks for the update")` — ticketId + trimmed body both verified | |
| AC-TST-4 | `submitTicketReply` — empty body validation | Test run: `npx jest ticket-reply.test.ts` | asserts `{ success: false, error }` and `replyToTicket` not called | PASS — 1 test passed | PASS — read `ticket-reply.test.ts:175-184`: FormData with `body: ""`; asserts `success: false`, `error` is truthy, and `mockReplyToTicket` was NOT called — confirms Zod validation fires before the lib call | |
| AC-TST-5 | `submitTicketReply` — SupportError code passthrough | Test run: `npx jest ticket-reply.test.ts` | asserts `{ success: false, error, errorCode: "ticket_not_open" }` | PASS — 2 tests passed (with and without code) | PASS — read `ticket-reply.test.ts:186-214`: two tests — one throws `SupportError("Ticket closed", 409, "ticket_not_open")` and asserts `errorCode: "ticket_not_open"`; the other throws `SupportError("Server error", 500)` (no code) and asserts `errorCode: undefined` — both passthrough directions covered | |

---

## Regression Acceptance Criteria

| AC | What | How | Pass | Agent | QC | Reviewer |
|----|------|-----|------|-------|----|----------|
| AC-REG-1 | All existing tests pass | Test run: `npm run test:ci` | All tests pass, 0 failures | PASS — 1127 tests, 97 suites, 0 failures | PASS — ran `npm run test:ci` and read output: `Test Suites: 97 passed, 97 total` / `Tests: 1127 passed, 1127 total` / `Time: 12.421s` — 32 more tests than the previous 1095 baseline (7 new ticket-reply tests + pre-existing growth), 0 failures | |
| AC-REG-2 | Precheck passes clean | Test run: `npm run precheck` | 0 TypeScript errors, 0 ESLint errors | PASS — 0 errors (1 pre-existing warning in `SalesClient.tsx`, unrelated to this branch) | PASS — ran `npm run precheck` and read output: `✖ 1 problem (0 errors, 1 warning)` — the warning is `react-hooks/incompatible-library` on `SalesClient.tsx:128`, present on `main` before this branch; 0 errors introduced by this change | |
