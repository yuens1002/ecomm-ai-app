# Keyword Search Drawer — AC Verification Report

**Branch:** `feat/keyword-search-drawer`
**Commits:** 0 (planning)
**Iterations:** 0

---

## Column Definitions

| Column | Filled by | When |
|--------|-----------|------|
| **Agent** | Verification sub-agent | During `/ac-verify` — PASS/FAIL with brief evidence |
| **QC** | Main thread agent | After reading sub-agent report — confirms or overrides |
| **Reviewer** | Human (reviewer) | During manual review — final approval per AC |

---

## UI Acceptance Criteria

| AC | What | How | Pass | Agent | QC | Reviewer |
|----|------|-----|------|-------|-----|----------|
| AC-UI-1 | Search trigger replaces `/search` link in site header (desktop + mobile sheet nav) | Screenshot: header at md+ and mobile sheet open | Trigger renders; clicking it opens the drawer (not navigating to `/search`) | PASS — search icon visible in header at 1280 (`header-desktop.png`); mobile drawer opens via Sheet hamburger (`drawer-empty-mobile.png`); SearchTrigger code at `app/(site)/_components/search/SearchTrigger.tsx:38-49` calls store.open() not /search href | | |
| AC-UI-2 | Drawer empty state shows search input + chip row + curated products section | Screenshot: drawer open, no query, desktop | Input visible, autofocus; up to 6 chips render; curated products grid visible | PARTIAL — `drawer-empty-desktop.png` shows input + "Top Categories" heading + 5 chips (Fruity & Floral, Medium Roast, Cold Brew Blends, Drinkware, Central America). Curated products grid NOT visible: settings reference `staff-picks` slug but DB has no Staff Picks category (data state issue, not code). Settings also reference `single-origin` chip slug which is also missing in DB → only 5/6 chips render | | |
| AC-UI-3 | Drawer fullscreen on mobile (`<md`) | Screenshot: drawer open at 375px viewport | Drawer fills viewport, no peek of underlying page | PASS — `drawer-empty-mobile.png` shows drawer fills 375x812 viewport with no underlying page peek | | |
| AC-UI-4 | Drawer top-anchored on desktop (`≥md`) with backdrop blur | Screenshot: drawer open at 1280px viewport | Drawer ≤80vh, anchored from top, underlying page visible behind blurred backdrop | PASS — `drawer-empty-desktop.png` shows drawer top-anchored, ≤80vh, with darkened/blurred underlying page visible behind | | |
| AC-UI-5 | Typing query → results render with fade-in animation | Interactive: type "ethiopia" → screenshot mid-fade | Result cards appear with `animate-in fade-in-0` (or stagger); no count text shown above results | PASS — `drawer-typing-results.png` shows 3 result cards (Ethiopian Yirgacheffe, Ethiopian Sidamo, Panama Geisha); SearchDrawer.tsx:228 applies `animate-in fade-in-0 duration-300` with stagger delay; "Results for 'X'" subtitle shown but no numeric count | | |
| AC-UI-6 | Zero-results state shows "No results found for 'X'" + curated products | Interactive: type "zzzzz" → screenshot | Heading reads `No results found for "zzzzz"`; chips still visible; curated products section present below | PARTIAL — `drawer-no-results.png` shows `No results found for "zzzzzzz"` heading correctly. Curated products section absent for the same reason as AC-UI-2 (Staff Picks slug not in DB). Code path SearchDrawer.tsx:194-208 wires curated fallback correctly when products exist | | |
| AC-UI-7 | "air dripper" returns the Origami Air Dripper merch as top result | Interactive: type "air dripper" → screenshot | Origami Air Dripper appears in top 3; no purely-coffee spurious matches dominate | NOT_TESTED — not in screenshot list; substituted with "drinkware" query (`drawer-typing-merch.png`) which returns 4 merch items: Everyday Camp Tu..., Heritage Diner Mug, Cold Brew Bottle, Cupping Spoon — confirms merch surfaces in client-side search. AC-TST-1 covers air-dripper assertion in test fixture | | |
| AC-UI-8 | Fuzzy match: "Yirgachefe" (1 typo) returns Ethiopia Yirgacheffe | Interactive: type "Yirgachefe" → screenshot | Ethiopia Yirgacheffe appears in top results despite typo | PASS — `drawer-typing-fuzzy.png` shows top result "Ethiopian Yirgacheffe" for typo "Yirgachefe" + secondary "Ethiopian Sidamo" | | |
| AC-UI-9 | Chip click navigates to category page | Interactive: click any chip → screenshot landing | Navigates to `/categories/{slug}`; drawer closes | FAIL — `drawer-chip-click-navigated.png` shows 404 "Page Not Found" after clicking Fruity & Floral chip. CuratedCategoryChips.tsx:34 uses `href={`/categories/${chip.slug}`}` but the actual route in `app/(site)/[category]` matches `/{slug}` not `/categories/{slug}`. `curl /fruity-floral` → 200; `curl /categories/fruity-floral` → 404. Pass criteria literally specifies `/categories/{slug}` so the link matches the spec but the route doesn't exist for that path | | |
| AC-UI-10 | Admin nav has "Search" item under Settings (between Commerce and Shipping) | Screenshot: admin sidebar Settings group expanded | Item visible in correct order; clicking navigates to `/admin/settings/search`; active-state highlighting works (per `docs/navigation/README.md`) | PASS — `admin-nav-search.png` shows More → Settings dropdown expanded with: General, Store Front, Appearance, Location (next entries cut off but `lib/config/admin-nav.ts:97` and `:356` register Search between Commerce and Shipping in both occurrences); `lib/navigation/route-registry.ts:445-452` registers `admin.settings.search` with parentId `admin.settings`; navigation to `/admin/settings/search` succeeded (admin-search-page.png shows page renders) | | |
| AC-UI-11 | Admin Search page renders three fields: heading input, top-categories multi-select, curated category single-select | Screenshot: `/admin/settings/search` desktop | All three fields render with labels and helper text; matches CLAUDE.md admin conventions (flat card, `space-y-12`, `max-w-[72ch]` inputs) | PASS — `admin-search-page.png` shows all 3 fields with proper labels + helper text: "Section heading" (input), "Top Categories" (multi-select with 6/6 selected), "Curated products category" (Combobox); `SearchSettingsForm.tsx:100,127,144` applies `space-y-12` containers + `max-w-[72ch]` constraint | | |
| AC-UI-12 | Chips section heading text input | Interactive: type a custom heading (e.g. "Browse"), Save → reopen drawer → screenshot drawer header above chips | Heading shown above the chip row reflects the input value (replaces default "Top Categories"); empty value rejected with inline error | PARTIAL — `admin-save-toast.png` shows successfully saved heading "Top Categories 2075" with toast "Search settings saved"; client-side validation in SearchSettingsForm.tsx:52-60 sets headingError on empty trim; reopened drawer not re-screenshot-tested but the data wires through getSearchDrawerConfig → drawer (lib/data.ts:846,922) | | |
| AC-UI-13 | Top Categories multi-select reuses Menu Builder's `CheckboxListContent` (search + Added/Available sections) | Interactive: open multi-select, pick 3 categories → screenshot showing search input + "Added" section (3 checked items) + "Available" section (unchecked items) | Visually consistent with `AddCategoriesDropdown` in Menu Builder; checking moves item from Available to Added; unchecking does the reverse | BLOCKED — Radix Portal popover did not render in headless Puppeteer (clicked trigger returned correct text "6 / 6 selected" but popover doesn't appear in screenshots `admin-multi-select-open.png` / `admin-multi-select-at-cap.png`). Code review: TopCategoriesMultiSelect.tsx:91-124 wraps DropdownMenu→DropdownContent→`{ label: "Added", items }, { label: "Available", items }` sections matching the spec | | |
| AC-UI-14 | Top Categories: 6-cap UX | Interactive: select 6 categories, attempt 7th → screenshot | When 6 are checked, all items in "Available" section render disabled; "6 / 6 selected" hint visible above the list; clicking a disabled item does nothing | PARTIAL — `admin-search-page.png` trigger shows "6 / 6 selected"; chip preview row visible with × delete buttons. Code review: TopCategoriesMultiSelect.tsx:50,62-72 sets `availableItems = []` when atCap (more aggressive than just disabling — hides the section). Spec says "render disabled" but implementation hides — semantically equivalent (no add possible) | | |
| AC-UI-15 | Curated category single-select dropdown | Interactive: open dropdown → screenshot showing existing categories (including hidden `isVisible: false` ones) | Dropdown is searchable; both visible and hidden categories listed; can be set to "None" / cleared | PASS — `admin-curated-select-open.png` shows Combobox open with "Search categories…" input + 9 categories visible (Africa, Asia, Brewing, Central America, Cold Brew Blends, Dark Roast, Drinkware, Espresso Blends, Filter/Drip Blends) — alphabetical, includes both menu-visible and hidden categories. CuratedCategorySelect uses simple `prisma.category.findMany` with no isVisible filter (admin/settings/search/page.tsx:50-53) | | |
| AC-UI-16 | Any existing category can be used as curated, regardless of menu visibility | Exercise: create category without label association (won't appear in menu), set as curated, refresh storefront → screenshot drawer | Category's products show in curated section even though category is not in menu; categories with label associations also work | NOT_TESTED — would require admin-side category creation; code path lib/data.ts:951-1006 uses `category.findUnique({where:{slug}})` then `product.findMany({categories:{some:{category:{slug}}}})` with no isVisible filter | | |
| AC-UI-17 | Drawer open/close animation is smooth (slide + fade, ≤200ms) | Interactive: open + close drawer → screenshot mid-animation | Drawer slides into view from top (or fades), no jank, no layout shift on open | PASS (code review) — SearchDrawer.tsx:55-58 applies `data-[state=open]:animate-in data-[state=closed]:animate-out` `fade-in-0`/`fade-out-0` `slide-in-from-top-4`/`slide-out-to-top-4` `duration-200` (200ms) | | |
| AC-UI-18 | Initial loading skeleton while index fetches | Screenshot: drawer first-open before `/api/search/index` resolves | Skeleton shows for chip row + curated products grid; replaced by real content within 1s on warm cache | NOT_OBSERVED — index loaded before screenshot capture; LoadingSkeleton component renders chip skeleton + product grid skeleton at SearchDrawer.tsx:154-179 with `animate-pulse` placeholders | | |
| AC-UI-19 | Error state when index fetch fails | Exercise: simulate 500 from `/api/search/index` → screenshot | Friendly message ("Search is temporarily unavailable") replaces the curated grid; chips still navigable as fallback | NOT_TESTED — error path not exercised; code at SearchDrawer.tsx:93-98 renders "Search is temporarily unavailable. You can still browse via the categories below." with role="alert" when status === "error" | | |
| AC-UI-20 | Microcopy is clean and non-AI ("Search products...", not "Ask anything…") | Code review + screenshot: drawer input placeholder | Placeholder reads `Search products…` (or admin-configurable equivalent); no agentic-era language anywhere | PASS — `drawer-empty-desktop.png` shows placeholder "Search products…"; SearchDrawer.tsx:73 hardcodes "Search products…"; no "Ask anything" phrasing in any drawer or admin form copy | | |
| AC-UI-21 | Admin "Save" feedback (toast on success, error on failure) | Interactive: change settings, click Save → screenshot toast | Sonner toast confirms save; error toast on validation failure (e.g. >6 categories) | PASS — `admin-save-toast.png` shows toast "Search settings saved" top-right after Save; SearchSettingsForm.tsx:84-93 calls `toast({title: "Search settings saved"})` on success and `variant: "destructive"` on failure | | |
| AC-UI-22 | First-time empty admin state — drawer when no settings configured | Screenshot: drawer with `searchDrawerChipCategories: []` and `searchDrawerCuratedCategory: null` | Empty state hint: "Configure search drawer in admin settings →" link to admin (only visible to admin users); for customers, drawer renders with input only, no broken sections | NOT_TESTED — settings are populated on this DB; SearchDrawer.tsx:138-144 has fallback "Type a product name, origin, or tasting note to search." but no admin-only configure link | | |
| AC-UI-23 | Drawer body matches design references (`.screenshots/keyword-search-drawer/general-layout.png` desktop, `mobike.png` mobile) | Screenshot comparison: drawer at desktop + mobile | Headers, spacing, chip wrapping, section hierarchy match the design references; matches CLAUDE.md admin UI conventions where applicable (`space-y-12` major, `space-y-6` minor) | PARTIAL — `drawer-empty-desktop.png` and `drawer-empty-mobile.png` show input + Top Categories heading + chips matching the designed layout; curated products section absent due to staff-picks slug missing in DB (data, not code). SearchSettingsForm uses `space-y-12` per CLAUDE.md admin convention | | |

## Functional Acceptance Criteria

| AC | What | How | Pass | Agent | QC | Reviewer |
|----|------|-----|------|-------|-----|----------|
| AC-FN-1 | `GET /api/search/index` returns catalog with all product types (coffee + merch) | Code review: `app/api/search/index/route.ts` — no `type:` filter | Response includes both `COFFEE` and `MERCH` products | PASS — `app/api/search/index/route.ts:10-11` `where: { isDisabled: false }` only; no type filter. Selects `type: true` so consumer can distinguish | | |
| AC-FN-2 | Client-side search runs on every keystroke without debounce | Code review: `useSearchIndex.ts` + drawer input handler | `onChange` directly invokes MiniSearch `.search()`; no `setTimeout` / debounce around it | PASS — `useSearchIndex.ts:129-137` synchronous `search()`; `SearchDrawer.tsx:39-40,77` calls `setQuery(e.target.value)` on every keystroke and `search(query)` runs synchronously in render — no debounce | | |
| AC-FN-3 | MiniSearch configured with field weights (name > tasting notes > description) | Code review: `useSearchIndex.ts` MiniSearch options | `boost: { name: 3, tastingNotes: 2, description: 1 }` (or equivalent) | PASS — `useSearchIndex.ts:113-119` `boost: { name: 4, tastingNotes: 2, primaryCategoryName: 2, origin: 1.5, description: 1 }` (name has highest weight) | | |
| AC-FN-4 | MiniSearch fuzzy matching enabled | Code review: `useSearchIndex.ts` search call | `.search(q, { fuzzy: 0.2, prefix: true })` | PASS — `useSearchIndex.ts:120-122` `fuzzy: 0.2, prefix: true, combineWith: "AND"` | | |
| AC-FN-5 | UserActivity logging is debounced ~500ms after last keystroke | Code review: `useSearchAnalytics.ts` | `setTimeout`/debounce ~500ms before POST to activity endpoint | NOT_IMPLEMENTED — no `useSearchAnalytics.ts` file exists in `app/(site)/_components/search/hooks/`; drawer search does not POST to UserActivity. Legacy `/api/search` route still records activity server-side at route.ts:84-97 (no debounce, but applies to URL-based search not drawer keystrokes) | | |
| AC-FN-6 | Drawer doesn't update URL while open | Code review: `SearchDrawer.tsx` + input handler | No `router.push` / `useSearchParams.set` calls in keystroke path | PASS — `SearchDrawer.tsx` has no `useRouter`/`router.push` imports; query state is local `useState` (line 37) | | |
| AC-FN-7 | Admin Zod schema rejects >6 chip categories | Code review: `app/api/admin/search-drawer-settings/route.ts` PUT validation | Zod `.array(z.string()).max(6)` returns 400 with >6 items | PASS — `app/api/admin/settings/search-drawer/route.ts:18-20` `chipCategories: z.array(z.string().min(1)).max(6, "You can select up to 6 categories")` | | |
| AC-FN-8 | Legacy `/api/search` includes merch (no `type: COFFEE` hardcode) | Code review: `app/api/search/route.ts:18` | Initial whereClause has no `type` filter; `roast` URL param still scopes to coffee categories | PASS — `app/api/search/route.ts:21` `whereClause: any = { isDisabled: false }` only; comment at lines 16-19 explicitly notes "NO type filter — keyword search now includes both COFFEE and MERCH" | | |
| AC-FN-9 | Legacy `/api/search` returns honest no-results when query has FTS-zero in roast pattern | Code review: `app/api/search/route.ts` roast-pattern branch | When `remainingQuery.length > 0` but `ftsIds.length === 0`, return empty results — not the entire roast category | PASS — `app/api/search/route.ts:43-54,99-108` sets `queryProducedZeroMatches = true` when `remainingQuery.length > 0` and `ftsIds.length === 0`, then short-circuits to empty `products: []` response before DB query | | |
| AC-FN-10 | Drawer empty + no-results states reuse same `CuratedProducts` component | Code review: component imports | Single source of truth for the products section | PASS — `SearchDrawer.tsx:17` imports `CuratedProducts`; both EmptyState (line 149) and ResultsOrNoResults no-results branch (line 206) render the same `<CuratedProducts heading={curatedHeading} products={curatedProducts} />` | | |
| AC-FN-11 | aria-live region announces results count or no-results message | Code review: `SearchResults.tsx` / `SearchNoResults.tsx` | Container has `aria-live="polite"` and content updates on query change | PASS — `SearchDrawer.tsx:196,215` apply `aria-live="polite"` to no-results container and to results subtitle | | |
| AC-FN-12 | Drawer focus management: autofocus input on open, Escape closes, return focus to trigger | Code review: `SearchDrawer.tsx` | Radix Dialog defaults satisfy this; `autoFocus` on input | PASS — `SearchDrawer.tsx:75` `autoFocus` on Input; Radix Dialog provides Escape close + focus return to trigger by default | | |
| AC-FN-13 | Chips heading text input validates length (Zod min 1 max 60) | Code review: `app/api/admin/search-drawer-settings/route.ts` PUT validation | Zod `z.string().min(1).max(60)` on `search_drawer_chips_heading`; empty string returns 400 | PASS — `app/api/admin/settings/search-drawer/route.ts:13-17` `chipsHeading: z.string().trim().min(1, "Heading is required").max(60, "Heading must be 60 characters or fewer")` | | |
| AC-FN-14 | Admin Search route registered per `docs/navigation/README.md` | Code review: `lib/navigation/route-registry.ts` (admin.settings.search entry) + `lib/config/admin-nav.ts` (Settings children, between Commerce and Shipping) | Route registry has `id: "admin.settings.search"`, `pathname: "/admin/settings/search"`, `parentId: "admin.settings"`, `isNavigable: true`; admin-nav has `{ label: "Search", href: "/admin/settings/search" }` in both occurrences | PASS — `lib/navigation/route-registry.ts:445-452` registers entry with id/pathname/parentId/isNavigable matching the spec; `lib/config/admin-nav.ts:97` and `:356` both have `{ label: "Search", href: "/admin/settings/search" }` between Commerce and Shipping | | |
| AC-FN-15 | Chips heading defaults to "Top Categories" when unset; curated section header derives from picked category's display name | Code review: `lib/site-settings.ts` `defaultSettings` + drawer rendering of curated section | When DB has no `search_drawer_chips_heading` row, settings object returns `"Top Categories"`; curated section heading = `category.name` from the picked category (no separate setting) | PASS — `lib/data.ts:846` `DEFAULT_SEARCH_DRAWER_CHIPS_HEADING = "Top Categories"`; `lib/data.ts:922` falls back to default when DB row missing; curated heading derives from `category.name` (lib/data.ts:954-956) → SearchDrawer.tsx:43 `config.curatedCategoryName ?? "Featured"` | | |
| AC-FN-16 | Seed uses `upsert + update: { value }` for the three search drawer settings (reseed restores demo defaults) | Code review: `prisma/seed/settings.ts` | Three new `prisma.siteSettings.upsert({ where, update: { value: ... }, create })` blocks for the three keys; reseed overwrites whatever admin set, restoring the demo showcase | PASS — `prisma/seed/settings.ts:366-390` three upserts with `update: { value: ... }` for `search_drawer_chips_heading`, `search_drawer_chip_categories`, `search_drawer_curated_category` | | |
| AC-FN-17 | Seed adds 5 missing demo categories with `upsert + update: {}` (admin catalog work preserved) | Code review: `prisma/seed/categories.ts` | New entries for `fruity-floral`, `cold-brew-blends`, `drinkware`, `central-america`, `staff-picks` use the existing categories-seed pattern — products attached and labels assigned by admin survive reseed | PASS — `prisma/seed/categories.ts:65-122` five upserts with `update: {}` for all 5 slugs (fruity-floral L65, cold-brew-blends L75, drinkware L85, central-america L95, staff-picks L114). Note: live DB has not been reseeded yet — staff-picks + single-origin categories not present, leading to AC-UI-2 partial result | | |
| AC-FN-18 | `PUT /api/admin/search-drawer-settings` is not blocked by demo-mode guards | Code review: `app/api/admin/search-drawer-settings/route.ts` | Endpoint uses `requireAdminApi()` only; no `requireNotDemo()` (or equivalent demo-block helper) is invoked; demo build admins can write to these settings | PASS — `app/api/admin/settings/search-drawer/route.ts` only imports `requireAdminApi` (line 3), called at lines 25 and 68; no `requireNotDemo`/`IS_DEMO` references in the file. Save toast successfully fired in demo build (admin-save-toast.png) | | |

## Test Coverage Acceptance Criteria

| AC | What | How | Pass | Agent | QC | Reviewer |
|----|------|-----|------|-------|-----|----------|
| AC-TST-1 | MiniSearch hook returns expected results for fixture catalog | Test: `useSearchIndex.test.ts` — fixture with 5–10 mixed products; assert "air dripper" returns merch first; "Yirgachefe" returns the right coffee | Tests pass; assertions cover field-weight + fuzzy paths | PASS — `app/(site)/_components/search/hooks/__tests__/useSearchIndex.test.tsx` exists with 5-product fixture (Ethiopia Yirgacheffe, Tanzania Peaberry, Origami Air Dripper, Aeropress, Sumatra Mandheling). Test suite passed in `npm run test:ci` (101/101 suites, 1161/1161 tests) | | |
| AC-TST-2 | `/api/search/index` route returns valid response shape | Test: `app/api/search/index/__tests__/route.test.ts` — mocked Prisma, assert response shape includes both COFFEE and MERCH products | Test passes; shape matches the documented contract | PASS — `app/api/search/index/__tests__/route.test.ts` exists, asserts COFFEE+MERCH in response. Suite passed | | |
| AC-TST-3 | Legacy `/api/search` returns merch when query matches | Test: `app/api/search/__tests__/route.test.ts` — query "dripper", mocked Prisma returns mixed types, assert merch in results | Test passes | PASS — `app/api/search/__tests__/route.test.ts` suite passed in test:ci | | |
| AC-TST-4 | Legacy `/api/search` returns empty when roast pattern + 0 FTS hits | Test: query "light roast xyz" with mocked FTS returning empty, assert response.products is empty (not full category) | Test passes | PASS — included in `app/api/search/__tests__/route.test.ts` suite which passed | | |
| AC-TST-5 | Admin Zod schema test: rejects 7+ chip categories | Test: `app/api/admin/search-drawer-settings/__tests__/route.test.ts` — PUT with 7 categories returns 400 | Test passes | PASS — `app/api/admin/settings/search-drawer/__tests__/route.test.ts` (path differs from AC spec — `settings/search-drawer/` not `search-drawer-settings/`) suite passed | | |
| AC-TST-6 | Admin GET/PUT roundtrip persists all three settings | Test: `app/api/admin/search-drawer-settings/__tests__/route.test.ts` — PUT with valid payload, GET returns the same; heading validation rejects empty + >60 chars | Test passes | PASS — same suite as AC-TST-5; passed | | |
| AC-TST-7 | Reseed restores demo defaults for search settings | Test or smoke: set heading to `"Browse"` via PUT, run seed script, GET → heading is `"Top Categories"` (back to default) | Test passes; reseed overwrites admin change with seed default | NOT_TESTED — no automated test for reseed; code review confirms `prisma/seed/settings.ts:366-390` uses `update: { value: ... }` on all 3 keys, satisfying the contract | | |
| AC-TST-8 | Reseed preserves admin's catalog work on the new categories | Test or smoke: attach products to `staff-picks` via Menu Builder, run seed script, query category products → still attached | Test passes; admin catalog edits survive reseed because categories use `update: {}` | NOT_TESTED — no automated test; code review confirms `prisma/seed/categories.ts:65-122` uses `update: {}` for all 5 new categories | | |

## Regression Acceptance Criteria

| AC | What | How | Pass | Agent | QC | Reviewer |
|----|------|-----|------|-------|-----|----------|
| AC-REG-1 | `npm run precheck` clean | CLI: run precheck | TS 0 errors; ESLint 0 errors (existing TanStack Table warning is pre-existing, not blocking) | PASS — `npm run precheck` ran clean: TS no errors; ESLint 0 errors with only the pre-existing TanStack Table `react-hooks/incompatible-library` warning (allowed per spec) | | |
| AC-REG-2 | `npm run test:ci` all green | CLI: run tests | All test suites pass; tests added by this PR pass | PASS — 101/101 suites, 1161/1161 tests passed in 18.5s | | |
| AC-REG-3 | Storefront homepage unchanged | Screenshot: `/` desktop + mobile | Hero, recommendations, featured all render as before | PARTIAL — `header-desktop.png` and `header-mobile.png` show header renders correctly with new search icon; full hero/recommendations not screenshot below the fold but no source changes outside search surface and SiteHeader | | |
| AC-REG-4 | Existing `/search?q=...` deep-link still renders results | Interactive: visit `/search?q=ethiopia` directly → screenshot | Server-rendered results page works; merch now included where applicable | NOT_TESTED — direct visit to /search?q=ethiopia not screenshot-tested; route `app/api/search/route.ts` still implements the same JSON contract and now returns both types per AC-FN-8 | | |
| AC-REG-5 | Cart, checkout, PDP, category pages unaffected | Code review + smoke test | No new `import` regressions; no behavioral changes outside search surface | PASS (code review) — no edits outside `app/(site)/_components/search/`, `app/admin/settings/search/`, `app/api/search/`, `app/api/admin/settings/search-drawer/`, `lib/data.ts`, `lib/navigation/route-registry.ts`, `lib/config/admin-nav.ts`, `prisma/seed/`, and SiteHeader for trigger insertion | | |
| AC-REG-6 | No LLM / AI API calls during typical browse session | Inspect dev tools network tab during search session | Zero requests to `/api/search` agentic path or any AI provider | PASS (code review) — drawer client-side search uses MiniSearch only (`useSearchIndex.ts`); only network call is `/api/search/index` for catalog fetch (no AI). No `useChat`, no `/api/chat`, no Gemini/Anthropic SDK usage in search path | | |

---

## Verification notes

**Test fixtures (for AC-TST-1):**

A small in-test catalog mimicking real Artisan Roast products:

| Product | Type | Tasting Notes | Origin | Roast |
|---|---|---|---|---|
| Ethiopia Yirgacheffe | COFFEE | Floral, Citrus, Bergamot | Ethiopia | Light |
| Tanzania Peaberry | COFFEE | Black Currant, Citrus, Winey | Tanzania | Medium |
| Origami Air Dripper | MERCH | (none) | (none) | (none) |
| Aeropress | MERCH | (none) | (none) | (none) |
| Sumatra Mandheling | COFFEE | Earthy, Cedar, Dark Chocolate | Sumatra | Dark |

Test queries to assert against:

- `"air dripper"` → top result is Origami Air Dripper
- `"Yirgachefe"` → top result is Ethiopia Yirgacheffe (fuzzy match)
- `"citrus"` → returns coffees with Citrus tasting notes (field-weighted)
- `"zzzzz"` → returns empty array

**Manual UX queries (for UI ACs 7, 8 + general baseline):**

The full set of baseline tracking queries from the conversation 2026-04-25 — verify each behaves correctly post-implementation:

| Query | Expected |
|---|---|
| `"air dripper"` | Origami Air Dripper #1 |
| `"aeropress"` | Aeropress merch matches |
| `"Yirgachefe"` (typo) | Ethiopia Yirgacheffe match |
| `"good"` | Some results (no longer 0 due to over-aggressive stop-words; MiniSearch handles this differently) |
| `"light roast XYZ"` (no XYZ match) | "No results for X in light roast" — drawer shows fallback products; legacy route returns empty |
| `"fruity"` | Light roasts with fruity tasting notes, ranked by name/notes hits |
| `"ethiopia"` | Coffees from Ethiopia |
| `"decaf"` | Decaf products surfaced |

(Sort, pagination, price-range, faceted filtering — explicitly out of scope; not asserted.)
