// Payment processor abstraction layer
// Common types used across all processors
export * from "./types";

// Processor-specific modules are imported directly:
// import { normalizeCheckoutSession } from "@/lib/payments/stripe";
